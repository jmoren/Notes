%tr
  %td= nota.title
  %td= nota.user.email
  %td= nota.created_at.to_s(:short)
  %td= link_to 'Show', [:admin, nota]
  %td= link_to 'Edit', edit_admin_nota_path(nota)
  %td= link_to 'Destroy', [:admin,  nota], :confirm => 'Are you sure?', :method => :delete

